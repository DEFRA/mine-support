{{- include "adp-helm-library.deployment" (list . "adp-demo-web.deployment") -}}
{{- define "adp-demo-web.deployment" -}}
spec:
  template:
    spec:
      containers:
      - {{ include "adp-helm-library.container" (list . "adp-demo-web.container") }}
{{- end -}}
